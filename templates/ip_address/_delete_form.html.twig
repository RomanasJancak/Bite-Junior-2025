<form method="post" action="{{ path('app_ip_address_delete', {'id': ip_address.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ip_address.id) }}">
    <button class="btn">Delete</button>
</form>
