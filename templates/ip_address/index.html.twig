{% extends 'base.html.twig' %}

{% block title %}IpAddress index{% endblock %}

{% block body %}
    <h1>IpAddress index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Ip</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ip_address in ip_addresses %}
            <tr>
                <td>{{ ip_address.id }}</td>
                <td>{{ ip_address.ip }}</td>
                <td>
                    <a href="{{ path('app_ip_address_show', {'id': ip_address.id}) }}">show</a>
                    <a href="{{ path('app_ip_address_edit', {'id': ip_address.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_ip_address_new') }}">Create new</a>
{% endblock %}
